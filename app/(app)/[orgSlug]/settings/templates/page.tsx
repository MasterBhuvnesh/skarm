"use client";

import { TemplateManager } from "@/components/issues/template-manager";

/**
 * Templates settings: create and edit issue templates, and schedule
 * recurring issues (rituals) from them.
 */
export default function TemplatesSettingsPage() {
  return <TemplateManager />;
}
